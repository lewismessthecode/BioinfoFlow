"""Shared enums and constants used across the application."""


class ApprovalStatus:
    """Approval status constants."""

    PENDING = "pending"
    APPROVED = "approved"
    REJECTED = "rejected"
