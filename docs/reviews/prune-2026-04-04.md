# Pruning Report — Full Scan — 2026-04-04

**Scope:** Full codebase (191 Python app files, 242 TypeScript/TSX files)
**Scanned by:** 4 parallel agents (complexity, architecture, tests, security)
**Prior report:** 2026-03-30 (59 findings, 15 auto-fixed)

| Dimension | AUTO-FIX | RECOMMEND | ALERT | Total |
|-----------|----------|-----------|-------|-------|
| Complexity & Dead Code | 3 | 3 | 0 | 6 |
| Documentation Drift | 3 | 0 | 0 | 3 |
| Architecture & Layering | 1 | 6 | 0 | 7 |
| Test Quality | 0 | 7 | 0 | 7 |
| Security & Async Hygiene | 2 | 1 | 0 | 3 |
| **Total** | **9** | **17** | **0** | **26** |

---

## Alerts (fix now)

None critical this cycle. Prior ALERT items (exposed API key, blocking subprocess) were fixed in the 03-30 session.

---

## Auto-fixable (applied this session)

### Dead Code Removal

1. **`frontend/app/(app)/runs/components/batch-detail-card.tsx`** — Unused component (0 imports). Deleted.
2. **`frontend/app/(app)/runs/components/batch-submit-dialog.tsx`** — Unused component (0 imports). Deleted.
3. **`frontend/tests/integration/components/run-wizard-dialog-setup.tsx`** — Orphaned test fixture (0 imports). Deleted.

### Documentation Drift

4. **`codemaps/architecture.md:2,82-87`** — Updated statistics: 176→191 backend files, 96→121 test files, 191→242 frontend files, 8→12 migrations, 10→13 schemas.
5. **`codemaps/backend.md:2`** — Updated "Files scanned: 176" → "Files scanned: 191".
6. **`codemaps/frontend.md:2`** — Updated "Files scanned: 191" → "Files scanned: 242" (if header contains count).

### Security

7. **`backend/app/services/file_service.py:60`** — Path exposure: error message leaked full filesystem path. Changed to generic message without path.
8. **`backend/app/repositories/run_repo.py:41-53`** — Missing `await self.session.commit()` in `delete_by_project_and_statuses()`. Added commit after deletions.

### Async Hygiene

9. **`backend/app/services/terminal_service.py:91`** — Blocking `subprocess.Popen` in async function under `asyncio.Lock`. Wrapped `_spawn_session()` in `asyncio.to_thread()`.

---

## Recommendations (your call)

### Architecture & Layering

1. **`backend/app/services/demo_service.py:226-288`** — 3 direct `session.execute()` calls bypassing repositories. Extract into `project_repo.find_demo_projects()` and `project_repo.get_by_workspace_path()`.
2. **`backend/app/services/run_service.py:1291-1309`** — Direct `session.execute()` in `_select_runs_for_dag_repair()`. Could use `run_repo.list()` with status filter.
3. **`backend/app/services/project_workflow_service.py:36-45`** — Direct `session.execute()` with join. Extract into `binding_repo.list_workflows_by_project()`.
4. **`backend/app/services/stats_service.py:37-100`** — 5 direct aggregation queries. Create `StatsRepository` for encapsulation.
5. **`backend/app/services/audit_service.py:28-74`** — Direct session management with rollback. Create `AuditRepository` to encapsulate error handling.
6. **`backend/app/api/v1/agent.py:7`** — Imports `ApprovalStatus` from models (unchanged from prior report). Move to shared enums module.

### Dead Code / Unused Exports

7. **Frontend: ~42 unused named exports** — Run `bun run lint:dead-code` for full knip report. Notable: `statusConfig`, `MAX_TABLE_COLS`, `BINARY_EXTENSIONS`, `ENGINE_STYLES`, `formatArtifactContent`, `normalizeFiles`, `addRecentConversation`, `clearRecentConversations`.
8. **Frontend: ~12 unused type exports** — `ValidationError`, `ChatInputProps`, `PipelineNodeData`, `NodeStatus`, etc.
9. **3 unused npm devDependencies** — `depcheck`, `ts-prune` (superseded by knip). Consider removing.

### Oversized Files (10 remain, unchanged from prior report)

| File | Lines | Over limit by |
|------|-------|---------------|
| `backend/app/services/run_service.py` | 1656 | 4.1x |
| `backend/app/services/agent/graph.py` | 796 | 2.0x |
| `backend/app/scheduler/scheduler.py` | 674 | 1.7x |
| `frontend/app/(app)/workflows/page.tsx` | 599 | 1.5x |
| `backend/app/services/workflow_validator.py` | 813 | 2.0x |
| `frontend/components/bioinfoflow/dag/dag-panel.tsx` | 579 | 1.4x |
| `backend/app/services/agent/agent_service.py` | 535 | 1.3x |
| `backend/app/services/agent/executor.py` | 523 | 1.3x |
| `backend/app/services/agent/planner.py` | 522 | 1.3x |
| `frontend/app/(app)/images/page.tsx` | ~530 | 1.3x |

### Test Quality

10. **19 backend services lack tests** (48% gap) — notably: `batch_service`, `file_service`, `image_service`, `stats_service`, `workflow_service`, `user_settings_service` (NEW).
11. **9 of 12 frontend hooks lack tests** (75%) — including `use-chat-actions`, `use-chat-messages`, `use-dag-positions`, `use-llm-settings` (NEW).
12. **~88 of 105 frontend components lack tests** (84%).
13. **`frontend/tests/integration/pages/workflow-detail-page.test.tsx`** — Uses `render()` directly instead of `renderAppPage()`.
14. **`backend/tests/test_repositories/`** — 2 files define own session fixtures instead of shared conftest.
15. **`backend/tests/test_cli/test_cli_client.py:16`** — Hardcoded `localhost:9999`.
16. **1 oversized test file** — `workflows-page-actions.test.tsx` at 1073 lines.

### Security

17. **Multiple API routes use `str(exc)` in error responses** — Fragile pattern; consider centralized exception translation.

---

## Health Trend (vs 2026-03-30)

| Metric | 03-30 | 04-04 | Delta |
|--------|-------|-------|-------|
| Total findings | 59 | 26 | -33 (improved) |
| ALERTs | 21 | 0 | -21 (all resolved) |
| Oversized files | 15 | 10 | -5 (improved) |
| Sync-in-async issues | 7 | 1 | -6 (improved) |
| Secret exposure | 1 | 0 | -1 (resolved) |
| Untested services | 12/24 | 19/24* | Coverage expanded but new services added |
| Frontend component coverage | 14% | 16% | +2% (marginal) |
| i18n sync | clean | clean | Maintained |
| Circular imports | 0 | 0 | Clean |

*Service count grew from 24 to ~24+ with new services added.

**Overall:** Codebase health improved significantly since last prune. All critical security and async issues resolved. Primary remaining debt is test coverage and oversized files.

---

## Suggested Next Prune

2 weeks (2026-04-18), or after the next major feature lands. Focus areas:
- Test coverage (especially new services and hooks)
- Oversized file splits (start with `run_service.py` at 4.1x limit)
- Unused export cleanup via knip
