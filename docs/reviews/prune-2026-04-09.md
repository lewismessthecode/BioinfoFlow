# Pruning Report — Full Scan — 2026-04-09

**Scope:** Full codebase (199 Python app files, 259 TypeScript/TSX files)
**Scanned by:** 4 parallel agents (complexity, architecture, tests, security)
**Prior report:** 2026-04-04 (26 findings, 9 auto-fixed)

| Dimension | AUTO-FIX | RECOMMEND | ALERT | Total |
|-----------|----------|-----------|-------|-------|
| Complexity & Dead Code | 0 | 8 | 0 | 8 |
| Documentation Drift | 3 | 0 | 0 | 3 |
| Architecture & Layering | 1 | 4 | 0 | 5 |
| Test Quality | 0 | 6 | 0 | 6 |
| Security & Async Hygiene | 0 | 2 | 1 | 3 |
| **Total** | **4** | **20** | **1** | **25** |

---

## Alerts (fix now)

1. **`backend/app/services/agent/conversation_manager.py:27-129`** — Race condition: `ConversationManager` modifies shared `_running` dict across async tasks with no `asyncio.Lock`. Concurrent `register`/`unregister`/`cancel` can race on the same `conversation_id`. Add `asyncio.Lock` to guard all dictionary operations.

---

## Auto-fixable (applied this session)

### Import Hygiene

1. **`backend/app/repositories/stats_repo.py:44`** — Deferred import of `Project` model inside `get_project_count()` moved to module top level. No circular dependency risk (verified).

### Documentation Drift

2. **`codemaps/architecture.md`** — Updated statistics: 191→199 backend files, 242→259 frontend files, 121→127 backend tests, 49→59 frontend tests.
3. **`codemaps/backend.md`** — Updated "Files scanned: 191" → "Files scanned: 199".
4. **`codemaps/frontend.md`** — Updated "Files scanned: 242" → "Files scanned: 259".

---

## Recommendations (your call)

### Architecture & Layering

1. **`backend/app/services/image_service.py:362-367`** — `_persist_image()` does manual `setattr` loop + `session.commit()` + `session.refresh()` in the service. Can't use `BaseRepository.update()` because it skips `None` values, and callers pass `error_message=None` to clear errors. Options: (a) add `update_all()` to BaseRepository that includes `None` values, or (b) add `persist()` method to `ImageRepository`.
2. **`backend/app/services/run_dag_service.py:289-293`** — Direct `session.commit()` after mutating `run.config` for DAG repair. Needs a `RunRepository.update_config()` method.
3. **`backend/app/services/run_dispatch.py:101-111`** — `_mark_run_failed()` opens its own session and commits directly. Should use a `RunRepository.mark_failed()` method.
4. **`backend/app/services/run_service.py:132`** — Direct `session.flush()` in `_require_workflow_enabled()`. Consider repo method for auto-binding.

### Complexity & Dead Code

5. **Oversized files (14 files >400 lines)** — 4 critical (>750 lines):

| File | Lines | Over limit by |
|------|-------|---------------|
| `backend/app/services/agent/agent_service.py` | 874 | 2.2x |
| `backend/app/services/workflow_validator.py` | 813 | 2.0x |
| `backend/app/services/run_service.py` | 795 | 2.0x |
| `backend/app/services/agent/runtime/llm_client.py` | 725 | 1.8x |
| `backend/app/scheduler/scheduler.py` | 655 | 1.6x |
| `backend/app/services/run_submission_service.py` | 609 | 1.5x |
| `backend/app/services/agent/tools/file_tools.py` | 589 | 1.5x |
| `frontend/app/(app)/workflows/components/workflow-register-dialog.tsx` | 783 | 2.0x |
| `frontend/app/(app)/images/page.tsx` | 666 | 1.7x |
| `frontend/app/(app)/workflows/page.tsx` | 607 | 1.5x |
| `frontend/components/bioinfoflow/dag/dag-panel.tsx` | 581 | 1.5x |

6. **`backend/app/api/v1/runs.py` — `_serialize()` at 484 lines** — Single function containing entire run response building with 5+ nesting levels. Critical maintenance risk.
7. **10 duplicate `_serialize` functions across API routes** — Nearly identical patterns in `runs.py`, `workflows.py`, `images.py`, `agent.py`, `projects.py`, `notifications.py`, `project_workflows.py`. Consider a shared serialization module.
8. **57 repetitive error handling blocks** across API routes — Same `try/except ValueError/FileNotFoundError` pattern. Consider a `@handle_errors` decorator.

### Security & Async Hygiene

9. **`backend/app/api/v1/files.py:106-112`** — Blocking `open()` + `read()` in sync generator inside async download endpoint. Use `aiofiles` for async streaming.
10. **Error leakage via `str(exc)` in 12+ API route files** — Still unfixed from prior scan. `str(exc)` in error responses could leak internal details. Consider centralized exception translation.

### Test Quality

11. **18 of 29 backend services lack tests (62% gap)** — Notable untested: `batch_service`, `run_submission_service`, `run_dag_service`, `run_dispatch`, `workspace_service`, `docker_service`, `notification_service`, `stats_service`.
12. **8 of 10 frontend hooks lack tests (80%)** — Only `use-events` and `use-onboarding` have tests.
13. **7 integration test files use bare `render()` instead of `renderAppPage()`** — `workflow-detail-page`, `run-detail-page`, `run-detail-content`, `terminal-dock`, `dag-panel`, `workflow-register-dialog`, `app-layout-terminal`.
14. **10 oversized test files (>400 lines)** — Largest: `workflows-page-actions.test.tsx` (1073 lines), `test_runs.py` (1014 lines).
15. **Hardcoded timestamps in 9 test files** — Fragile to time-based changes. Use date factories.
16. **`backend/tests/test_cli/test_cli_client.py:16`** — Hardcoded `localhost:9999` (unchanged from prior).

---

## Health Trend (vs 2026-04-04)

| Metric | 04-04 | 04-09 | Delta |
|--------|-------|-------|-------|
| Total findings | 26 | 25 | -1 (stable) |
| ALERTs | 0 | 1 | +1 (new race condition found) |
| Oversized files | 10 | 14 | +4 (codebase grew, new files crossed threshold) |
| Layer violations (session in service) | 5 fixed | 4 new | Prior batch resolved, new ones found |
| Sync-in-async issues | 1 | 1 | Stable (files.py download, different from prior) |
| Error leakage pattern | unfixed | unfixed | Persists |
| Backend test coverage gap | 19/24 untested | 18/29 untested | +1 tested, but service count grew |
| Frontend hook test gap | 9/12 untested | 8/10 untested | Slight improvement |
| Frontend test files | 49 | 59 | +10 (good momentum) |
| Backend test files | 121 | 127 | +6 |
| i18n sync | clean | clean | Maintained |
| Circular imports | 0 | 0 | Clean |

**Overall:** Codebase is growing (199+259 = 458 files, up from 191+242 = 433). Prior layer violations were resolved but new ones appeared as features were added. The ConversationManager race condition is the first ALERT since 03-30. Test coverage is improving in absolute numbers (+16 test files) but the gap persists as new services are added faster than tests. Oversized file count increased — some files like `agent_service.py` grew from 535 to 874 lines.

---

## Suggested Next Prune

2 weeks (2026-04-23), or after the next major feature. Priority focus areas:
- **ALERT: ConversationManager race condition** — fix before next prune
- **Oversized file splits** — start with `agent_service.py` (874 lines, grew 63% since last prune)
- **Layer violation cleanup** — add `update_all()`/`mark_failed()` repo methods
- **Test coverage** — prioritize `run_submission_service`, `run_dispatch`, `batch_service`
