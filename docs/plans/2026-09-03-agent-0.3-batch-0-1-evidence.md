# Agent 0.3 Batch 0/1 Evidence

This file records the stabilization evidence and repository boundaries. It
does not restate the implementation plan.

## Baseline and boundaries

- Worktree: `/Users/lewisliu/Dev/ACTIVE/BioinfoFlow/.worktrees/agent-0-3-stabilization`
- Branch: `codex/agent-0-3-stabilization`
- Code baseline at start: `aaad2913e8ad8572cf67fb1daf02766aa98241b0`
  (`ci: require agent browser checks`)
- Parent batch commit retained: `4573051c1` (`test: isolate backend test host`)
- `origin/main` and `main`: `320803aa644ad4930d57ca9316aa8e896fee22f2`
- No rebase, PR creation, merge, or import from `main` was performed.
- Stabilization commits, in order: `9197438` (`ci: expand agent browser
  coverage`), `85ebe1d` (`test: stabilize agent browser assertions`), and
  `0e89336` (`test: add agent shell screenshots`).
- The session-start dirty state contained only the tracked generated
  `frontend/playwright-report/index.html` and
  `frontend/playwright-report/results.xml`; they were inspected, preserved,
  and excluded from every functional commit.
- The session-start `main` snapshot listed untracked files under the
  stabilization worktree's `docs/`; none of that dirty state was copied into
  this branch. The current `main` worktree re-check is clean.

## Batch 0 acceptance

- `frontend/package.json` now routes the required agent browser command through
  `agent-first-analysis.spec.ts`, `agent-workflow-run.spec.ts`, and
  `agent-workspace-shell.spec.ts` only.
- The required CI job installs the pinned Node sandbox worker dependencies
  before starting the backend.
- Existing agent E2E assertions were aligned with the public plan popover DOM
  and the mobile accessible offline status without changing product behavior.

## Batch 1 acceptance

- The agent shell keeps its existing geometry and interaction assertions.
- `expect(page).toHaveScreenshot(...)` covers the deterministic shell baseline
  at 1440x900, 1024x900, and 390x844 Chromium viewports.
- Screenshot capture disables animations, hides the caret, masks the dynamic
  workspace data and project name, and uses a fixed mask color.
- The three committed baselines are:
  - `frontend/tests/e2e/agent-workspace-shell.spec.ts-snapshots/agent-workspace-shell-1440x900-chromium-darwin.png`
  - `frontend/tests/e2e/agent-workspace-shell.spec.ts-snapshots/agent-workspace-shell-1024x900-chromium-darwin.png`
  - `frontend/tests/e2e/agent-workspace-shell.spec.ts-snapshots/agent-workspace-shell-390x844-chromium-darwin.png`

## Verification evidence

All commands ran from this worktree unless noted:

- `npm ci` in `backend/sandbox_worker`: passed; 18 packages installed and
  zero vulnerabilities reported.
- `bun run lint`: passed.
- `actionlint .github/workflows/ci.yml`: passed.
- Focused `git diff --check` over the changed CI, package, and agent test
  files: passed.
- Focused screenshot baseline generation at 1440x900, 1024x900, and 390x844:
  each passed one shell test.
- Focused structured-plan agent test: passed.
- Complete required agent browser command, with the isolated default ports
  (`frontend=3100`, `backend=8100`, `model=9100`):
  - desktop 1440x900: 14 passed
  - tablet 1024x900: 14 passed
  - mobile 390x844: 14 passed

The full repository E2E suite and full backend test suite were intentionally
not run for this focused stabilization batch.

## Generated artifacts

Playwright regenerated the tracked report files while verifying the suite.
They remain outside the commits so existing user or parent-thread intent is
not overwritten:

- `frontend/playwright-report/index.html`
- `frontend/playwright-report/results.xml`

The stale `baseline-browser-mapping` notices emitted during the Next build are
warnings only and did not affect the focused results.
